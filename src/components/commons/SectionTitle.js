import React from "react";

const SectionTitle = ({ title }) => {
	return (
		<div className="section-title mt-1 mb-5">
			<h2 className="text-center mb-3">{title}</h2>
			<div></div>
		</div>
	);
};

export default SectionTitle;
